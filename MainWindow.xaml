<Window x:Class="dsh_app.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:helpers="clr-namespace:dsh_app.Helpers"
        mc:Ignorable="d"
        Title="DeepSeek Harness"
        Width="1200" Height="800"
        MinWidth="960" MinHeight="640"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BgWindowBrush}"
        Icon="{x:Null}"
        WindowStyle="None"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="36"
                      CornerRadius="0"
                      GlassFrameThickness="0"
                      ResizeBorderThickness="5"
                      UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- ==================== 顶栏（WindowChrome 自绘标题栏） ==================== -->
        <Grid x:Name="TitleBar"
              Grid.Row="0"
              Background="{DynamicResource BgWindowBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="DeepSeek Harness"
                       Margin="16,0,0,0"
                       FontSize="13"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       VerticalAlignment="Center" />

            <!-- 工具按钮 -->
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        Margin="12,0,0,0">
                <Button x:Name="TitleBtnRefresh"
                        Content="刷新"
                        Style="{DynamicResource IconButton}"
                        Click="TitleBtnRefresh_Click" />
                <Button x:Name="TitleBtnBrowser"
                        Content="浏览器"
                        Style="{DynamicResource IconButton}"
                        Click="TitleBtnBrowser_Click" />
                <Button x:Name="TitleBtnRestart"
                        Content="重启服务"
                        Style="{DynamicResource IconButton}"
                        Click="TitleBtnRestart_Click" />
                <Button x:Name="TitleBtnMenu"
                        Content="菜单"
                        Style="{DynamicResource IconButton}"
                        Click="TitleBtnMenu_Click" />
                <!-- 余额显示（菜单右侧，非工具按钮；点击手动刷新；仅在设置开启时可见）：
                     IconButton 样式提供 hover 点击反馈（背景 HoverBgBrush），
                     与顶栏工具按钮同高 26 同中心，严格一条线 -->
                <Button x:Name="TitleBalance"
                        Height="26"
                        Margin="8,0,4,0"
                        Padding="8,0"
                        VerticalAlignment="Center"
                        RenderTransformOrigin="0.5,0.5"
                        Style="{DynamicResource IconButton}"
                        Foreground="{DynamicResource TextWeakBrush}"
                        WindowChrome.IsHitTestVisibleInChrome="True"
                        PreviewMouseLeftButtonDown="TitleBalance_PreviewMouseDown"
                        Click="TitleBalance_Click"
                        Visibility="Collapsed">
                    <TextBlock x:Name="TitleBalanceText"
                               MaxWidth="80"
                               FontSize="12"
                               TextTrimming="CharacterEllipsis" />
                </Button>
            </StackPanel>

            <!-- 余额刷新状态卡（锚定余额按钮下方；Popup 独立 HWND，不受 airspace 影响） -->
            <Popup x:Name="BalanceStatusPopup"
                   Placement="Bottom"
                   PlacementTarget="{Binding ElementName=TitleBalance}"
                   AllowsTransparency="True"
                   StaysOpen="False"
                   IsOpen="False">
                <Border Padding="12,8"
                        Background="{DynamicResource BgCardBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                    <TextBlock x:Name="BalanceStatusText"
                               FontSize="12"
                               Foreground="{DynamicResource TextSecondaryBrush}" />
                </Border>
            </Popup>

            <!-- 菜单下拉（Popup 独立 HWND，不受 WebView2 airspace 影响） -->
            <Popup x:Name="MenuPopup"
                   Placement="Bottom"
                   PlacementTarget="{Binding ElementName=TitleBtnMenu}"
                   AllowsTransparency="True"
                   StaysOpen="False">
                <Border Width="220"
                        Padding="4"
                        Background="{DynamicResource BgCardBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                    <StackPanel>
                        <Button Content="关于"
                                Style="{DynamicResource MenuItemButton}"
                                Click="MenuAbout_Click" />
                        <Button Content="设置"
                                Style="{DynamicResource MenuItemButton}"
                                Click="MenuSettings_Click" />
                        <Button Content="日志"
                                Style="{DynamicResource MenuItemButton}"
                                Click="MenuLog_Click" />
                        <Button x:Name="MenuCheckUpdate"
                                Content="检查更新"
                                Style="{DynamicResource MenuItemButton}"
                                Click="MenuCheckUpdate_Click" />
                    </StackPanel>
                </Border>
            </Popup>

            <!-- 系统按钮 -->
            <StackPanel Grid.Column="3"
                        Orientation="Horizontal">
                <Button x:Name="TitleBtnMin"
                        Content="─"
                        ToolTip="最小化"
                        Style="{DynamicResource SysButton}"
                        Click="TitleBtnMin_Click" />
                <Button x:Name="TitleBtnMax"
                        Content="□"
                        ToolTip="最大化"
                        Style="{DynamicResource SysButton}"
                        Click="TitleBtnMax_Click" />
                <Button x:Name="TitleBtnClose"
                        Content="✕"
                        ToolTip="关闭"
                        Style="{DynamicResource SysButtonClose}"
                        Click="TitleBtnClose_Click" />
            </StackPanel>
        </Grid>

        <!-- ==================== 内容区 ====================
             四周留出 WindowChrome 的 resize 热区（普通状态 5px）；
             WebView2 的 HWND 会拦截热区命中测试，必须让出边缘才能拖拽调窗。 -->
        <Grid x:Name="ContentRoot"
              Grid.Row="1"
              Margin="5,0,5,5">
            <!-- Harness Web UI（初始隐藏：避免 HWND 子窗口盖住启动覆盖层，页面加载完成后再显示） -->
            <wv2:WebView2 x:Name="WebView" Visibility="Collapsed" />

            <!-- 状态 / 错误覆盖层：
                 中央 = 品牌区（正常）或错误区（失败，替换品牌）
                 左下 = 启动进度卡片（低调，不喧宾夺主） -->
            <Grid x:Name="Overlay"
                  Panel.ZIndex="10"
                  Background="{DynamicResource OverlayBgBrush}"
                  Visibility="Collapsed">

                <!-- 中央品牌区 -->
                <StackPanel x:Name="CenterBrand"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    <TextBlock Text="DeepSeek Harness"
                               FontSize="40"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               HorizontalAlignment="Center" />
                    <TextBlock x:Name="CenterSubtitle"
                               Margin="0,12,0,0"
                               Text="正在启动…"
                               FontSize="13"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               HorizontalAlignment="Center" />
                </StackPanel>

                <!-- 中央错误区（失败时替换品牌区） -->
                <StackPanel x:Name="CenterError"
                            MaxWidth="560"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Center">
                        <TextBlock x:Name="OverlayGlyph"
                                   Text="⚠"
                                   FontSize="20"
                                   Foreground="{DynamicResource AccentOrangeBrush}"
                                   VerticalAlignment="Center" />
                        <TextBlock x:Name="OverlayTitle"
                                   Margin="12,0,0,0"
                                   Text="启动失败"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                    <TextBlock x:Name="OverlayErrorDetail"
                               Margin="0,14,0,0"
                               TextWrapping="Wrap"
                               LineHeight="22"
                               FontSize="13"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               TextAlignment="Center" />
                    <StackPanel x:Name="OverlayActions"
                                Orientation="Horizontal"
                                Margin="0,20,0,0"
                                HorizontalAlignment="Center"
                                Visibility="Collapsed">
                        <Button x:Name="RetryButton"
                                Content="重试"
                                Padding="24,9"
                                Click="RetryButton_Click"
                                Style="{DynamicResource RoundedButton}"
                                Background="{DynamicResource ButtonGreenBrush}"
                                Foreground="White"
                                BorderThickness="0" />
                        <Button x:Name="LogButton"
                                Content="打开日志"
                                Margin="12,0,0,0"
                                Padding="24,9"
                                Click="LogButton_Click"
                                Style="{DynamicResource RoundedButton}"
                                Background="{DynamicResource BgCardBrush}"
                                Foreground="{DynamicResource TextPrimaryBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1" />
                    </StackPanel>
                </StackPanel>

                <!-- 左下角启动进度卡片（半透明，不喧宾夺主） -->
                <Border Width="400"
                        Margin="20,0,0,20"
                        Padding="20,16"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Bottom"
                        Background="{DynamicResource CardTranslucentBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="10">
                    <StackPanel>
                        <TextBlock Text="启动进度"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   Margin="0,0,0,8" />
                        <ItemsControl x:Name="StepList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="18" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Icon}"
                                                   FontSize="13"
                                                   Foreground="{Binding IconBrush}" />
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Name}"
                                                   Margin="10,0,0,0"
                                                   FontSize="13"
                                                   Foreground="{DynamicResource TextPrimaryBrush}" />
                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding Detail}"
                                                   Margin="12,0,0,0"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   TextTrimming="CharacterEllipsis" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <ProgressBar x:Name="OverlayProgress"
                                     Margin="0,10,0,0"
                                     Height="3"
                                     IsIndeterminate="True"
                                     Foreground="{DynamicResource AccentBlueBrush}"
                                     Background="{DynamicResource TrackBgBrush}" />

                        <!-- 实时日志（Toolbox 风格自定义滚动条） -->
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="16" />
                            </Grid.ColumnDefinitions>
                            <ScrollViewer x:Name="OverlayLogScroller"
                                          MaxHeight="88"
                                          VerticalScrollBarVisibility="Hidden">
                                <TextBlock x:Name="OverlayDetail"
                                           TextWrapping="Wrap"
                                           LineHeight="16"
                                           FontSize="11"
                                           Foreground="{DynamicResource TextSecondaryBrush}" />
                            </ScrollViewer>
                            <helpers:CustomScrollBar Grid.Column="1"
                                                     Width="16"
                                                     TargetScrollViewer="{Binding ElementName=OverlayLogScroller}" />
                        </Grid>

                        <TextBlock Text="dsh-app v1.1 · DeepSeek Harness 桌面壳"
                                   Margin="0,8,0,0"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextWeakBrush}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
